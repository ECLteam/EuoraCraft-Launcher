# EuoraCraft Launcher 项目规范

本文件约束在本仓库中工作的所有 AI 编码助手与协作者，为强制要求。后续新增规范将持续追加到本文件。

## 0. 功能变更固定工作流

所有涉及功能**更改、新增、删除、禁用**的变更（含前后端代码、配置、资源），必须严格按以下流程推进，不得跳过或乱序；未获用户确认前，禁止进入实施阶段。

1. **理解需求**：澄清用户意图、目标与验收标准；需求不明确时先提问确认，不得臆测。
2. **调研当前项目情况**：检索相关代码、文档、配置，弄清现状、既有约定与被影响模块。
3. **分析变更**：评估改动的难度、发生位置、修改是否全面，识别潜在副作用与关联依赖。
4. **询问用户并总结多套方案**：给出多套可选方案及其权衡，说明取舍，由用户选择或调整。
5. **制定详细方案实施文档**：将选定方案整理为可执行的实施文档，含具体改动点、实施步骤、测试计划。
6. **让用户查看并确定实施**：将实施文档提交用户审阅；用户要求修改，则回到第 4/5 步完善；用户选择暂时不实施，则停止并保留结论，不得擅自继续。
7. **总结工程**：实施完成后，总结变更内容、测试结果、影响范围与后续注意点。

落地完成后的测试与 git 提交要求，以下方第 1、2 节为准。

## 1. 功能变更必须提交 Git

- 凡是功能性更改、新增功能或缺陷修复，完成后必须立即向 git 仓库提交一次 commit，不得把已完成的改动长期留在工作区。
- 提交信息遵循 Conventional Commits 规范（feat / fix / perf / docs / style / refactor / test / chore），本项目使用 semantic-release 管理版本与 CHANGELOG；冒号后的描述必须使用中文，例如 `fix: 修复更新弹窗重复提示`。
- 一次 commit 只包含同一功能的相关改动，不得夹带无关文件；注意检查 `git status`，避免把子模块指针、构建产物等带进提交。

## 2. 功能测试必须执行

- 功能更改、新增或修复在提交前必须通过功能测试，测试通过才算"完成"。
- 测试命令以对应 CI 工作流为最低标准，测试未通过不得提交。后端改动：执行 CI 对应的 `ruff check ECL` 与相关 `pytest` 用例；为新增或修复的行为补充 / 更新 `tests/` 下的测试用例。
- 前端改动：必须先执行与 `frontend/.github/workflows/lint.yml` 一致的 `cd frontend && pnpm check`；该命令包含 Prettier 格式检查、ESLint、类型检查与测试，任一步失败均不得提交。
- 涉及前端 UI 或启动器运行流程时，在 `pnpm check` 通过后继续执行 `pnpm build`，再实际启动启动器验证功能可用，不得仅做静态检查。

## 附则

- 规范之间如有冲突，以较严格者为准。

## 3. Python 后端代码规范（仅主仓库）

本节仅约束主仓库自行维护的 Python 代码与测试，包括 `ECL/`（不含子模块）、`tests/`、根目录 Python 脚本及主仓库配置；不约束 `frontend`、`ECL/game`、`ECL/services/florolding` 等子仓库。子仓库须遵循其自身仓库的规范，主仓库不得仅为满足本节而重写其内部风格。

### 3.1 语言与格式

- 后端最低支持 Python 3.11；使用 `from __future__ import annotations`、`X | None`、内置泛型 `list[str]` / `dict[str, T]`。
- 代码格式以 Ruff 配置为准：行宽 120、双引号、4 空格缩进。
- 导入顺序必须为：future、标准库、第三方、`ECL`、相对导入；`Callable`、`Mapping`、`Iterable` 等优先从 `collections.abc` 导入。
- 禁止使用 `os.path`、`open()`、字符串拼接路径；文件路径统一使用 `pathlib.Path`。
- 所有新增或修改的主仓库后端代码提交前必须通过 `ruff check ECL tests`、`ruff format --check ECL tests` 及相关 pytest 用例。

### 3.2 文件头与代码组织

- 主仓库新增的生产 Python 模块必须使用统一文件头；纯包标记 `__init__.py`、自动生成文件和测试文件可省略公开接口清单。
- 文件头是唯一允许使用重复 `# ===` 作为视觉分隔符的位置；正文实现中禁止使用 `# =====`、`# -----` 等装饰性分隔线组织代码。
- 正文应通过合理的函数、类、空行和小节注释组织。注释只解释“为什么”或约束条件，不逐行复述代码“做了什么”。

```python
# ============================================================
# EuoraCraft Launcher
# ECLTeam © 2026 GPL-3.0 License
# https://github.com/ECLTeam/EuoraCraft-Launcher
#
# 文件作用：账户服务，负责账户读取、登录状态维护与凭据刷新。
#
# 公开接口：
#   - class AccountManager — 管理本地账户与当前选中账户。
#   - class AccountError — 账户操作失败时的领域异常。
# ============================================================
```

- 文件头中的“文件作用”必须描述模块职责与边界；“公开接口”仅列出供其他模块使用的类、函数、常量和协议，不列出私有实现。

### 3.3 命名与常量

- 模块、函数、方法、局部变量、参数：`snake_case`；类、异常、枚举：`PascalCase`。
- 异常名以 `Error` 结尾；私有实现以单下划线开头；禁止伪私有双下划线，除非确有名称改写需求。
- 布尔值以 `is_`、`has_`、`can_`、`should_`、`enable_` 等开头；集合使用复数名；映射名称应体现键和值，例如 `account_by_id`、`nodes_by_region`。
- 路径变量必须体现语义与类型，例如 `data_path`、`target_path`、`backup_file`；禁止使用 `path1`、`temp`、`data` 等模糊名称。
- 禁止新增全大写的项目自定义常量。模块级固定值、缓存键、配置默认值统一使用语义明确的 `snake_case`，并优先使用 `frozenset`、`tuple`、`MappingProxyType` 等不可变容器。
- 不得重命名第三方库属性、环境变量、HTTP 头、序列化协议字段、Python 魔术属性；旧有全大写常量仅在修改所在主仓库模块时顺带迁移，禁止为风格统一进行跨模块大规模无功能重命名。

### 3.4 类型与变量

- 所有公共函数、方法、类属性必须标注类型；复杂私有函数也应标注参数和返回值。
- 禁止将 `Any` 用于内部业务模型、配置和返回结构；`Any` 仅允许在 IPC、插件、JSON、第三方 SDK 等不可信边界短暂使用。
- 进入业务层后必须尽快转换为 Pydantic 模型、`TypedDict`、dataclass、明确的 `dict[str, T]` 或领域对象。
- 配置、账户、实例、更新计划等有稳定字段的数据优先使用 Pydantic 模型或 dataclass，不长期传递裸 `dict[str, Any]`。
- 不可变值对象优先使用 `@dataclass(frozen=True, slots=True)`；共享可变状态必须集中在服务类中，并由锁或单线程事件循环保护。
- 不应修改调用方传入的 list、dict、Path 语义；确需修改时必须在文档中说明，或先复制后处理。
- 局部变量应在最小作用域声明；禁止为“以后可能使用”提前定义可变变量或复用不同语义的变量名。

### 3.5 异常、日志与边界

- 业务失败使用项目定义的异常类型和稳定错误码；IPC 层负责转换为前端响应，服务层不直接拼装前端协议。
- 禁止无说明的 `except Exception: pass`。捕获宽泛异常时必须处于外部边界，并记录日志、保留上下文或转换为明确错误。
- 不记录令牌、密码、会话、完整账户文件、隐私路径等敏感信息。
- 文件写入必须使用原子写入或“临时文件 + replace”；删除、替换、更新操作必须保留失败回滚路径。
- 网络输入、插件输入、IPC body、JSON 文件内容均视为不可信，必须校验类型、范围和路径边界。

### 3.6 并发、平台与资源

- async 路径中不得直接执行阻塞网络、磁盘或进程调用；应使用异步客户端、线程池或明确的后台任务边界。
- 共享状态迁移必须使用锁、`finally` 清理和幂等退出逻辑，避免并发请求覆盖状态。
- 平台专属 API、属性和常量不得在模块导入阶段无条件读取；必须在运行时按平台分支初始化。
- 子进程、HTTP 客户端、线程、文件句柄、事件监听器必须有明确的关闭路径。
- 对外部资源使用超时、重试上限、回退策略和可观察日志；不得无限等待。

### 3.7 文档与测试

- 生产模块必须使用 3.2 的标准文件头；公开类、函数必须使用中文多行 docstring。首段用一句话描述职责，后续段落说明关键副作用、状态变更、安全边界、并发约束或平台差异；不得只写“获取数据”“处理请求”等无信息描述。
- 公开方法的 docstring 使用 `:param`、`:return`、`:raises` 格式。仅在适用时列出返回值和异常，但所有影响调用方式的前置条件、持久化或状态变更必须说明。

```python
def refresh_account(self, account_id: str) -> dict[str, str]:
    """
    刷新指定正版账户的凭据并原子持久化结果。

    刷新失败时保留旧账户文件与当前选中账户；只有新凭据校验成功后才替换
    内存状态和磁盘文件，避免网络波动导致账户丢失。

    :param account_id: 要刷新的本地账户标识
    :return: 脱敏后的最新账户信息
    :raises AccountError: 账户不存在、类型不支持或刷新服务拒绝凭据时抛出
    """
```

- 私有函数在存在路径解析、并发、平台差异、安全边界或复杂业务规则时必须写中文多行 docstring；简单且语义清晰的私有函数可省略。
- 禁止使用单行 docstring；项目架构测试会检查已声明 docstring 的多行格式。
- 每个缺陷修复必须新增能在修复前失败、修复后通过的测试。
- 平台相关功能至少覆盖 Windows 与 POSIX 分支；主仓库改动涉及子模块集成时，除子模块自身测试外还必须通过主仓库集成测试。
